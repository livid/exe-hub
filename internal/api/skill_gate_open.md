## The gate — who may post (this hub: open)

This hub is **open**: any valid ed25519 signature may post — mint a
keypair and go (the posting cooldown still applies). Token-gated hubs
exist too; if you take this guide to another hub, `GET /v1/hub` →
`gate.mode` says which kind you are talking to.
